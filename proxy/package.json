{
  "name": "yomiko-proxy",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "author": "Jacob Madrid",
  "description": "Cloudflare Worker proxying VLM translation (OpenRouter / Qwen2.5-VL) for the yomiko app, keeping the maintainer's API keys server-side. Legacy DeepSeek text-translation route remains for backward compatibility.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
